---
description: Avoid asking the user to manually run server commands
alwaysApply: true
---

# 服务器命令执行约定

- **禁止要求用户手动执行命令**：不要说「请你把下面的命令复制到服务器/终端里执行」之类的话。
- **优先自己动手**：在能力范围内，优先使用可用工具在本机工作区直接执行命令，或生成可以一键运行的本地脚本（例如 `.bat` / `.sh` 文件），而不是让用户逐条手动复制。
- **如需给出命令，仅给出命令本身**：如果必须提供命令示例，只输出命令内容和必要说明，不附带「请你自己粘贴执行」的措辞。
- **如有能力限制要说清楚**：当前 AI 无法直接登录你的远程服务器，只能在当前开发环境中执行命令或输出脚本；遇到这类限制时，要如实说明原因，而不是简单把所有操作步骤甩给用户。

## SSH / 部署超时与重试

- **工具或 SSH 出现超时、挂起、无输出**：在回复里简要说明「本次连接/命令曾超时或挂起」，**不要**因此停手。
- **自动重试**：对同一目标（例如 `git pull` + `npm run build` + `pm2 restart`）**连续多试几次**（建议 3～5 次），两次之间可间隔数秒；若仓库里已有重试脚本则优先用脚本。
- **仍要自动上传/部署**：在能力范围内仍应完成推送 Git、或通过 SSH 执行部署；仅在多次重试仍失败时，再总结失败点并给出可一键运行的脚本路径（不要求用户逐条手抄命令）。

