# 构建时注入（可选）
# 复制为 .env.production 后填写（与高德 Web Key 同应用下的「安全密钥」）
VITE_AMAP_SECURITY_CODE=

# 运行时注入（推荐，无需为改密钥重新 npm run build）
# 在启动 lily-chat-web 的进程环境里设置，例如：AMAP_SECURITY_CODE=你的安全密钥
# server.js 会在返回 index.html 时写入 window.__AMAP_SECURITY_CODE__
