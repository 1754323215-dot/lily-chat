# 🌐 服务器配置说明

## 当前配置

### 后端服务器
- **IP 地址**: `139.129.194.84`
- **端口**: `8082`
- **完整地址**: `http://139.129.194.84:8082/api`

### 配置文件位置
`constants/config.js`

### 当前配置内容
```javascript
export const API_BASE_URL = 'http://139.129.194.84:8082/api';
```

## ✅ 配置已更新

服务器地址已更新为正确的端口 8082。

## 📝 使用说明

在代码中使用 API：

```javascript
import { API_BASE_URL } from '../constants/config';

// 示例：发送请求
fetch(`${API_BASE_URL}/your-endpoint`)
  .then(response => response.json())
  .then(data => console.log(data));
```

## 🔍 验证配置

确保后端服务正在运行：
- 访问：`http://139.129.194.84:8082/api`
- 应该能看到 API 响应或错误信息（说明连接正常）

## ⚠️ 注意事项

1. **HTTPS**: 如果生产环境使用 HTTPS，需要修改为 `https://`
2. **CORS**: 确保后端服务器允许跨域请求
3. **网络**: 确保手机和服务器网络连通

